# Binary Annotator Pro - Environment Variables

# Ollama Configuration
# URL where Ollama is running (use host.docker.internal for Docker Desktop)
OLLAMA_URL=http://host.docker.internal:11434

# MCP Configuration Path
# Path to your .mcp.json file (default: ~/.mcp.json)
MCP_CONFIG_PATH=~/.mcp.json

# Database Configuration
# Path where the SQLite database will be stored
DATABASE_PATH=/app/data/ecg_data.db

# Backend Port
PORT=3000

# Frontend API URL
VITE_API_URL=http://localhost:3000
